export enum TabKey {
  TRIP = 'trip',
  PERSONALIZE = 'personalize',
}

export enum ColumnType {
  BOOLEAN = 'boolean',
  TEXT = 'text',
  IMAGE = 'image',
  CURRENCY = 'currency',
  DATE_RANGE = 'dateRange',
  DATE = 'date',
  ARRAY = 'array',
  ACTIONS = 'actions',
  CUSTOM = 'custom',
}
